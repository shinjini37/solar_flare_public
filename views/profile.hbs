<!DOCTYPE html>
<html>
    <head>
        <title> {{title}} </title>
        {{> links_partial}}
    </head>
    <body>
        <!---->
        {{> top_body_partial}}
        <div class="main-body-container">
            <div class="nav-profile">
                {{{username}}}
            </div>
            <div class="main-view">
                <div class="viewer">
                </div>  
            </div>
            <div class="nav-music">
                {{#if user_own_profile}}
                    <div class="recent profile-recent">
                        {{{mine}}}
                    </div>                 
                    <div class="recent profile-recent">
                        {{{recents}}}
                    </div>   
                {{else}}
                    <div class="recent">
                        {{{recents}}}
                    </div> 
                {{/if}}
                
            </div>
        </div>
    </body>
</html>