<!DOCTYPE html>
<html>
    <head>
        <title> {{title}} </title>
        {{> links_partial}}
    </head>
    <body>
        <!---->
        {{> top_body_partial}}
        <div class="main-body-container">
            <div class="nav-profile">
                {{username}}
            </div>
            <div class="main-view">
                <div class="viewer">
                </div>  
            </div>
            <div class="nav-music">
                <div class="user_recent">
                    {{{mine}}}
                <div>                 
                <div class="recent">
                    {{{recents}}}
                </div>    
            </div>
        </div>
    </body>
</html>